version: "3.8"
services:
  db:
    image: postgres
    restart: always
    env_file:
      - .env.development
    ports:
      - 5440:5432

  redis:
    container_name: japahubs_redis
    image: redis:latest
    ports:
      - 6379:6379
